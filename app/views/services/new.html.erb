<%= form_for @service do |f| %>
	<%= f.label :service_name %>
	<%= f.text_field :service_name %>

	<%= f.submit %>
<% end %>
<%= link_to "Return to Services", services_path %> 
<%= link_to "Return to User Panel", user_path(session[:user_id]) %> 
