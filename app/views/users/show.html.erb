<h1><%= @user.first_name %> <%= @user.last_name %></h1>

<p>
	<%= @user.email %><br>
	<% if @user.admin == true %>
		You are an admin!
	<% end %>
</p>

<section class="admins">
	<% users = User.all %>
	<table>
		<tr class="table_title"><td><%= link_to "admins:", admins_path %></td></tr>
		<tr class="column_names">
			<td>Name</td>
			<td>Quotes Added</td>
		</tr>
		<% users.each do |user| %>
			<% count = Quote.where(user_id: user.id).count %>
			<% if user[:admin] == true %>
	    		<tr>
	    			<td><%= link_to "#{user[:first_name]} #{user[:last_name]}", admin_path(user[:id]) %></td>
	    			<td><%= count %></td>
	    		</tr>

	    	<% end %>
		<% end %>	
	</table>
</section>

<section class="services">
	<% services = Service.all %>
	<% total = 0 %>
	<table>
		<tr class="table_title"><td><%= link_to "services:", services_path %></td></tr>
		<tr class="column_names">
			<td>Service Name</td>
			<td>Quote #</td>
			<td>Quote Chosen?</td>
			<td>Price</td>
		</tr>
		<% services.each do |s| %>
			<% c = "No" %>
			<% chosen = Quote.find_by_service_id_and_chosen(s.id, true) %>
			<% if chosen != nil %>
				<% c = "Yes" %>
			<% end %>
			<% count = Quote.where(service_id: s.id).count %>

			<tr>
				<td><%= link_to "#{s.service_name}", service_path(s[:id]) %></td>
				<td><%= count %></td>
				<td><%= c %></td>
				<td>
					<% if c == "Yes" %>
						<%= chosen.price %>
						<% total += chosen.price %>
					<% end %>
				</td>
			</tr>
		<% end %>
		<tr><td>Total: <%= total %></td></tr>
	</table>
</section>

