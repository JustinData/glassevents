<h1><%= @user.first_name %> <%= @user.last_name %></h1>

<p>
	<%= @user.email %><br>
	Admin: <%= @user.admin %>
</p>

<section class="admins">admins:</section>
<ul>
	<% users = User.all %>
	<% users.each do |user| %>
		<% if user[:admin] == true %>
    		<li><%= user[:first_name] %> <% user[:last_name] %></li>

    	<% end %>
	<% end %>
	
</ul>

<section>Services, do it yo</section>
<section>tasks section (Y U no do this yet?!?)</section>

<section>payments section (Y U no do this yet?!?)</section>

<p>User, Admin, and Owner pannels will need to be different, at least partially.  Perhaps the same sections with differnt interactions.</p>
